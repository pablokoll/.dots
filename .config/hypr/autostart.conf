# Extra autostart processes
# exec-once = uwsm-app -- my-service
# exec-once = sleep 3 && /usr/lib/nordvpn/norduserd

# Allow user pablo-shopimasters to connect to this X/Xwayland display (Playwright MCP headed)
exec-once = bash -c 'for i in {1..30}; do xhost +si:localuser:pablo-shopimasters && break; sleep 1; done'
